services:
  webmcp:
    build: .
    ports:
      - "127.0.0.1:4797:4797"
    environment:
      WEBMCP_BIND_HOST: 0.0.0.0
      WEBMCP_CONFIG_DIR: /data
    volumes:
      - webmcp-state:/data
    command: ["node", "build/index.js", "--foreground", "--port", "4797"]
volumes:
  webmcp-state:
